FRONTEND_PORT=3000
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000/api
# When the frontend renders on the server (SSR), it can use API_BASE_URL.
# For Docker, point to http://backend:5000/api so requests use the internal network.
API_BASE_URL=http://localhost:5000/api
